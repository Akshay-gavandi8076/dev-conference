export type RoomData = {
  name: string
  description: string
  githubRepo: string
  tags: string
}

export type UpdateRoomData = {
  id: string
  userId: string
  name: string
  description: string
  githubRepo: string
  tags: string
}
